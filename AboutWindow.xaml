<Window x:Class="StoryTracker.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StoryTracker"
        mc:Ignorable="d"
        Background="{DynamicResource BackgroundColor}" Foreground="{DynamicResource ForegroundColor}"
        Title="AboutWindow" Height="450" Width="800">
    <Grid Margin="20">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="Story Tracker" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center"/>
            <TextBlock Text="Version 2.3.0" Margin="0,5,0,0" HorizontalAlignment="Center"/>

            <TextBlock Text="A desktop application for writers to track and edit their stories."
                   TextWrapping="Wrap"
                   Margin="0,15,0,0"
                   HorizontalAlignment="Center"/>

            <TextBlock Margin="0,20,0,0" HorizontalAlignment="Center">
            <Hyperlink NavigateUri="https://github.com/neogentrics/StoryTracker" RequestNavigate="Hyperlink_RequestNavigate">
                View Project on GitHub
            </Hyperlink>
            </TextBlock>

            <TextBlock Text="Copyright © 2025 Joshua Thomas" Margin="0,15,0,0" HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</Window>
