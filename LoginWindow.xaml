<Window x:Class="StoryTracker.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"   
        Background="{DynamicResource BackgroundColor}" Foreground="{DynamicResource ForegroundColor}"
        Title="Database Connection" Height="400" Width="400"
        WindowStartupLocation="CenterScreen">

    <DockPanel Margin="15">

        <StatusBar DockPanel.Dock="Bottom" 
           Background="{DynamicResource BackgroundColor}" 
           Foreground="{DynamicResource ForegroundColor}">
            <StatusBarItem>
                <TextBlock Name="LoginStatusTextBlock" Text="Ready"/>
            </StatusBarItem>
        </StatusBar>

        <Grid>
            <StackPanel>
                <TextBlock Text="Saved Connections:"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <ComboBox Name="SavedConnectionsComboBox" Grid.Column="0" Margin="0,5,5,0" SelectionChanged="SavedConnectionsComboBox_SelectionChanged"/>
                    <Button Name="DeleteButton" Grid.Column="1" Content="🗑️" Width="30" Margin="0,5,0,0" Click="DeleteButton_Click"/>
                </Grid>

                <TextBlock Text="Host / IP Address:" Margin="0,15,0,0"/>
                <TextBox Name="HostTextBox" Margin="0,5,0,0"/>

                <TextBlock Text="Database:" Margin="0,10,0,0"/>
                <TextBox Name="DatabaseTextBox" Margin="0,5,0,0"/>

                <TextBlock Text="User:" Margin="0,10,0,0"/>
                <TextBox Name="UserTextBox" Margin="0,5,0,0"/>

                <TextBlock Text="Password:" Margin="0,10,0,0"/>
                <PasswordBox Name="PasswordBox" Margin="0,5,0,0"/>

                <CheckBox Name="SaveCheckBox" Content="Save connection details" Margin="0,15,0,0" Foreground="White"/>

                <StackPanel Orientation="Horizontal" Margin="0,10,0,0" HorizontalAlignment="Center">
                    <Button Name="ConnectButton" Content="Connect" Width="80" Margin="5" Click="ConnectButton_Click"/>
                </StackPanel>
            </StackPanel>
        </Grid>
    </DockPanel>
</Window>